apiVersion: v1
kind: PersistentVolume
metadata:
  name: music-categorizer-pv
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/music-categorizer-data"